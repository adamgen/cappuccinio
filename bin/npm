#!/bin/bash
cd "$(dirname "$0")" || exit

bump(){
  cd ../static/src || exit 1
  local type=$1

  npm version prerelease --preid "$type"
}

publish(){
  cd ../static/src || exit 1
  npm publish
}



cmd=$1
shift
$cmd "$@"
